spring:
  application:
    name: razorpay

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/razorpay}
    username: ${DB_USERNAME:rajnishkushwaha}
    password: ${DB_PASSWORD:Ranjit24}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 9090


vault:
  master-key: hjwg238usvuyfe6ikvjsdhypd9836f32oidehv283762036djvhg02ekiouyfertof3297ffryfhdsve7

payment:
  simulator:
    poll-interval-ms: 5000
    chaos-mode: NORMAL
    methods:
      card:
        min-delay-seconds: 2
        max-delay-seconds: 6
        success-rate: 90

      UPI:
        min-delay-seconds: 1
        max-delay-seconds: 3
        success-rate: 95

      NET_BANKING:
        min-delay-seconds: 4
        max-delay-seconds: 10
        success-rate: 80


